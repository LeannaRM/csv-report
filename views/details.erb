<!DOCTYPE html>
<html>
<head>
	<link rel="stylesheet" type="text/css" href="/style.css">
	<link href="https://fonts.googleapis.com/css?family=Dancing+Script|Raleway" rel="stylesheet">
	<link href="https://fonts.googleapis.com/css?family=Anton" rel="stylesheet">
	<title>Account details for <%= @name %></title>
</head>
<body>

	<div class="header--container"> 
		<h1 class="header--title">Accounts Management</h1>
		<div class="headerbuttons--container">
			<form action="/index" class="headerform--buttons"><input type="submit" value="Main Menu" class="header--buttons"></form>
			<form action="/summary" class="headerform--buttons"><input type="submit" value="Account Summary" class="header--buttons"></form>
			<form action="/details" class="headerform--buttons"><input type="submit" value="Account Details" class="header--buttons"></form>
			<form action="/editTransaction" class="headerform--buttons"><input type="submit" value="Add New Transaction" class="header--buttons"></form>
			<form action="/logout" method="post" class="headerform--buttons"><input type="submit" value="Logout" class="header--buttons"></form>
		</div>
	</div>


	<%if @message %>
		<h3><%= @message %></h3>
	<% end %>

	<h3>Account Details for <%= @name %></h3>
	<table>
	<thead>
		<tr>
			<th>T#</th>
			<th>Account</th>
			<th>Date</th>
			<th>Payee</th>
			<th>Category</th>
			<th>Outflow</th>
			<th>Inflow</th>
		</tr>
	</thead>
	<% count = 0 %>
	<% @transactions.each do |row| %>
		<% count = count + 1 %>
		<tr>
			<td><%= count.to_s + ": " %></td>
			<% currentRow = row.split(",")
			currentRow.each do |x| %>
				<td><%= x %> </td>
			<% end %>
		</tr>
	<% end %>
	</table>
</body>
</html>